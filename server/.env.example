RESEND_API_KEY=re_your_key_here
NOTIFICATION_FROM_EMAIL=Poko <onboarding@resend.dev>

# Generate both with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
POKO_ADMIN_SECRET=generate_a_random_string_here
# Only used if POKO_DEV_MODE=1; otherwise real Google OAuth is required
POKO_DEV_TOKEN=generate_another_random_string_here

CLAUDE_BINARY=claude
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
